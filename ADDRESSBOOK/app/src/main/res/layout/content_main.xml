<?xml version="1.0" encoding="utf-8"?>
<FrameLayout android:id="@+id/fragmentContainer"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_vertical_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".MainActivity"
    />

    <!--Phone Layout: content_main.xml
    MainActivity dynamically displays the app’s Fragments
    in the FrameLayout named fragmentContainer.
    This layout fills the available space in MainActivity’s
    layout with 16dp padding on all sides.
    The app:layout_behavior property is used by activity_main.
    xml’s CoordinatorLayout to manage interactions between
    its views.
    Setting this property ensures that the contents of
    the FrameLayout scroll below the Toolbar defined
    in activity_main.xml.-->
